import sys
input = sys.stdin.readline

a, b = map(int, input()[::-1].split())
if a > b:
    print(a)
else:
    print(b)

input()으로 입력받고 [::-1] 로 문자열을 뒤집고 .split() 으로 공백으로 분리해서 a, b에 각각 할당
if 문으로 a b 비교해서 큰 숫자 출력